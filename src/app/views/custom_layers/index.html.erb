<div class="m-content">

    <div class="m-article">

        <div class="m-layermaker">
            <div class="header">
                <h1>
                    Be a Layers of London Layermaker
                </h1>

                <p>
                    We've a lot of hard work to do to get our historic maps look more accurate.
                    You can help out by fixing some old maps.
                </p>
            </div>

            <%= render "#{user_signed_in? ? "user_layers" : "layers"}_header" %>

            <div class="current-projects">
                <h2>
                    Current mapping projects
                </h2>

                <div class="layermaker-project">
                    <a href="/layermaker/RAF">RAF Project</a>
                </div>

                <% @layers.each do |layer| %>
                    <div class="layermaker-project">
                        <h3>
                            <%= link_to layer.title, georeferencer_project_path(layer.id) %>
                        </h3>
                    </div>
                <% end %>
            </div>
        </div>

    </div>

</div>