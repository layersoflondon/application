<div class="m-layermaker-stats">
    <!--div class="user-stats">
        <span class="header">
            You have completed:
        </span>

        <span class="count">

        </span>

        <span class="footer">
            images
        </span>
    </div-->

    <div class="leaderboard">
        <% @contributors.each do |contributor| %>
            <div class="leaderboard-row <%= current_user == contributor ? 'is-current-user' : '' %>">
                <div class="rank"><%= contributor.ranking %></div>
                <div class="count"><%= contributor.score %></div>
                <div class="name"><%= contributor.name %></div>
            </div>
        <% end %>
    </div>
</div>