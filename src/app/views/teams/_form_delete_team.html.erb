<%= form_tag("/teams/#{@team.id}" , method: "delete") do %>
  <button type="submit" class="delete-team" data-confirm="Are you sure you want to delete this team? All users will be removed.">Delete team</button>
<% end %>