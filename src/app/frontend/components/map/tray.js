import React, {PureComponent} from 'react';

class Tray extends PureComponent {
  constructor(props) {
    super(props)
  }

  render() {
    return <div id="tray-container">tray</div>;
  }
}

export default Tray;
